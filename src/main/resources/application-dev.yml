spring:
  # MySQL 数据源（开发环境）
  datasource:
    url: jdbc:mysql://192.168.234.130:3306/mamba_search?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai&allowMultiQueries=true
    username: root
    password: 123456
    driver-class-name: com.mysql.cj.jdbc.Driver

  # Redis（开发环境）
  data:
    redis:
      host: localhost
      port: 6379
      password:
      database: 0
      lettuce:
        pool:
          max-active: 8
          max-idle: 8
          min-idle: 0

  # Elasticsearch（开发环境 - 如未安装 ES 请注释掉）
  elasticsearch:
    uris: http://192.168.234.130:9200
    connection-timeout: 3s
    socket-timeout: 30s

# 腾讯云 COS（开发环境 - 按需填写真实密钥）
cos:
  secret-id: default
  secret-key: default
  bucket-name: default
  region: ap-guangzhou
