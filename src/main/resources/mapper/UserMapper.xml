<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.rc.javabackendtemplate.mapper.UserMapper">

    <resultMap id="UserResultMap" type="com.rc.mambasaerchbackend.model.entity.User">
        <id column="id" property="id"/>
        <result column="username" property="username"/>
        <result column="password" property="password"/>
        <result column="nickname" property="nickname"/>
        <result column="email" property="email"/>
        <result column="phone" property="phone"/>
        <result column="avatar" property="avatar"/>
        <result column="role" property="role"/>
        <result column="status" property="status"/>
        <result column="last_login_time" property="lastLoginTime"/>
        <result column="created_at" property="createdAt"/>
        <result column="updated_at" property="updatedAt"/>
        <result column="deleted" property="deleted"/>
    </resultMap>

    <sql id="baseColumns">
        id, username, password, nickname, email, phone, avatar,
        role, status, last_login_time, created_at, updated_at, deleted
    </sql>

    <select id="selectByUsername" resultMap="UserResultMap">
        SELECT <include refid="baseColumns"/>
        FROM `user`
        WHERE username = #{username} AND deleted = 0
    </select>

    <select id="selectByCondition" resultMap="UserResultMap">
        SELECT <include refid="baseColumns"/>
        FROM `user`
        WHERE deleted = 0
        <if test="query.username != null and query.username != ''">
            AND username LIKE CONCAT('%', #{query.username}, '%')
        </if>
        <if test="query.email != null and query.email != ''">
            AND email LIKE CONCAT('%', #{query.email}, '%')
        </if>
        <if test="query.phone != null and query.phone != ''">
            AND phone = #{query.phone}
        </if>
        <if test="query.role != null and query.role != ''">
            AND role = #{query.role}
        </if>
        <if test="query.status != null">
            AND status = #{query.status}
        </if>
        ORDER BY created_at DESC
    </select>

</mapper>
